[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.install]
cmds = ["mvn clean package -DskipTests", "ls -la target/"]

[start]
cmd = "java -jar target/logistics-routing-1.0.0.jar || java -jar target/*.jar"

